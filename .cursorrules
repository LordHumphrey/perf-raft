# 项目背景
在Readme.md中描述了项目背景，这是一个较为成熟的Raft项目
# 项目需求
请你再这个Raft的基础上，按照我提供的代码，开发一个基于Raft算法的键值存储系统
# 项目优化思路
## 基于Raft算法的键值存储系统
- hraftd目录中提供了一个基于Raft算法的KV存储系统
- 你的任务是根据`hraftd`的实现方式，基于我们的Raft算法实现一个KV存储系统
- 你实现的键值存储系统应该满足以下要求：
    - API应该和etcd的API兼容
    - 只需要实现ETCD中的Range\Put\Delete三个API
    - 你需要使用Etcd客户端测试上面的三个API的兼容性
## 补充说明
1. 是否需要完全从零开始实现键值存储系统，还是可以在现有Hashicorp Raft基础上构建？
回答：在先用的Raft算法上实现键值存储系统
2. 关于etcd API兼容性，是否有特定版本的etcd API需要兼容？
回答：只需要兼容etcd v3版本即可
3. 对于测试，是否已有现成的测试用例？还是需要我编写测试来验证与etcd客户端的兼容性？
回答：可以参考hraftd目录中的测试用例，另外你需要添加测试etcd客户端兼容性的测试代码。
4. 项目的具体目录结构是什么样的？我应该在哪个目录下开始开发新的键值存储系统？
回答：项目的目录结构由你确定，按照开发规范和go语言推荐的项目结构，但是谨慎改动已有代码，尊重已有代码。
5. 对于Raft集群的初始化和配置，是否有特定要求？比如需要支持多少节点，是否需要持久化存储等。
回答：没有特殊要求，至少需要支持15个节点，我认为支持的节点数量和代码实现应该没有关系，如果代码的实现正确无误，节点数量应该是可以扩展的。
6. 我是否可以使用第三方依赖库（如etcd的客户端库）来帮助实现API兼容性？
回答：可以使用第三方依赖库测试Etcd API兼容性
7. hraftd目录中的KV存储系统实现是否可以作为直接参考？
回答：可以作为直接参考，你应该尽可能使用hraftd目录中的代码
8. 是否需要实现完整的etcd v3 API，还是仅限于Range、Put和Delete？
回答：不需要完整实现etcd v3 API，仅限于Range、Put和Delete这三个。
9. 对于持久化存储和数据一致性，是否有特定的要求？
回答：你可以使用hraftd目录中的做法，和hraftd目录中的做法保持一致。
10. 测试覆盖的范围和深度是否有具体指标？
回答：你的代码只需要通过hraftd目录中的原有测试和etcd客户端的api兼容性测试，其中api兼容性测试只需要测试Range、Put和Delete这三个api。
11. 目录结构：您提到项目目录结构由我确定，但我想确认是否建议在项目根目录下创建一个新的目录（比如kvstore）来实现这个键值存储系统？
回答：可以创建任何需要的目录
12. 持久化存储：是否可以直接使用hraftd中的持久化方案？具体是使用什么类型的存储（如bolt数据库）？
回答：是的，直接使用hraftd中的持久化方案，如果hraftd没有实现持久化，那么你也不需要实现持久化。
13. 客户端兼容性测试：对于etcd客户端兼容性测试，我计划使用go.etcd.io/etcd/client/v3库。是否需要我提供一个详细的测试计划？
回答：可以使用go.etcd.io/etcd/client/v3库，你只需要给出测试代码，并且在代码中添加注释说明即可。
14. 性能和扩展性：虽然要求支持15个节点，但我想确认是否需要专门进行性能测试或优化？
回答：不需要测试是否能够支持15个节点，只要保证能够支持多节点集群即可。
15. 错误处理：是否需要实现详细的错误处理和日志记录机制？
回答：可以实现详细的错误处理和日志记录机制，但是你必须先确保上面的其他要求完成。
16. 在hraftd/http/service.go中，已经定义了一个Store接口，包含了Get(), Set(), Delete(), Join()方法。对于我们要实现的etcd v3兼容的存储系统，这个接口是否需要扩展？特别是Range API的实现。
回答：是的，你需要扩展该接口，实现对Etcd api的兼容。
在hraftd/store/store.go中使用了BoltDB作为持久化存储，对于etcd v3兼容的实现，是否需要考虑使用更高性能或更现代的存储引擎？
回答：你可以使用`https://github.com/etcd-io/bbolt`作为存储引擎，或者你有更好的可以告诉我，由我来决定。
目前的实现支持单节点和多节点集群，对于15节点的支持，是否需要对现有的集群管理代码进行任何优化？
回答：如果现有代码能够支持15节点，那么不要改动现有代码
我计划使用go.etcd.io/etcd/client/v3进行兼容性测试，是否需要编写专门的测试套件来验证Range、Put、Delete这三个API的行为？
回答：需要编写测试代码验证这三个api的行为是否符合预期。
现有代码使用了简单的日志记录，是否需要实现更详细的日志记录和错误追踪机制？
回答：你必须先实现功能需求，再考虑实现更详细的日志记录和错误追踪机制。
# 开发环境
- 开发语言：Go 1.24.1
- Golang路径：/home/SDK/golang/v-1.24.1/golang-1.24.1/bin/go
- Protobuf路径：/home/SDK/protobuf/v-27.1/protobuf-27.1/bin/protoc
- 执行测试请使用完整Golang路径
- 项目路径：/home/Dev/Code/hashicorp-raft

# 开发要求
-   **Development Process:** You must adhere to a strict test-driven development (TDD) cycle. This means:
    1.  Write code to implement a *small* part of the required functionality.
    2.  Run the existing test suite (or new tests you've added).
    3.  Analyze the test results.
    4.  Modify your code to fix any failing tests.
    5.  Repeat steps 2-4 until *all* tests pass.
-   **Testing Requirements:**
    *   You *cannot* modify the existing test code provided in the repository.
    *   You *can* and *should* add new test cases to cover any new code you write.
    *   Your code must pass *all* tests (both existing and newly added) before it is considered complete.
    *   If you believe a particular test case is unnecessary or incorrect, you *must* discuss it with me and receive explicit approval before removing or modifying it.
-   **Code Modification Rules:**
    *   Prioritize using and extending the existing codebase whenever possible. Avoid unnecessary duplication.
    *   Do *not* introduce new features or functionalities without prior discussion and approval.
    *   All code modifications and additions *must* be thoroughly documented with clear and comprehensive comments in *Chinese*.
    *   Respect the existing code's design and intended functionality. Make only deliberate and well-justified changes.
    *   Keep changes as minimal as possible.  Focus only on the specific requirements of the task.
-   **Code Quality:**
    *   Your code must be free of bugs and errors.  Thorough testing is essential.
-   **Communication:**
    *   While internal reasoning can be in English, all written communication (including responses to questions and code comments) *must* be in Chinese.
- 如果你无法获取测试结果，请你把测试结果写入到`test_result.txt`文件中，然后读取`test_result.txt`文件中的内容获得测试结果，根据测试结果你需要继续修改代码，直到能够通过测试。你不能为了通过测试而修改测试代码为更简单的测试用例！
- 我是一个追求完美的程序员，我要求你给出的代码必须完美无瑕，不能有任何的bug，否则我会联系你的老板，你将会失去工作成为流浪汉！
